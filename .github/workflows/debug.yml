name: Debug
on:
  # push:
  workflow_dispatch:

jobs:
  debug:
    # runs-on: macos-15-intel # 4C14G
    runs-on: macos-15-large
    steps:
      - uses: actions/checkout@v4
      - uses: fawazahmed0/action-debug-vscode@main # touch continue
